# Example config for TextProcessor
# (processor.simple_processor.text.text_processor.TextProcessor.build)
#
# Splits text into overlapping chunks and embeds each chunk.

embedder_config: embedder_config.yaml

chunking:
  size: 800       # chunk length in characters
  overlap: 100    # overlap between adjacent chunks in characters
