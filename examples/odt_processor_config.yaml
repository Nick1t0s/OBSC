# Example config for ODTProcessor
# (processor.complex_processor.odt.odt_processor.ODTProcessor.build)
#
# Paths are resolved relative to this config file's directory.
# Requires LibreOffice (`soffice`) on PATH for .odt -> .pdf conversion.

pdf_processor_config: pdf_processor_config.yaml
soffice: soffice
