# Example config for ODGProcessor
# (processor.complex_processor.odg.odg_processor.ODGProcessor.build)
#
# Paths are resolved relative to this config file's directory.
# Requires LibreOffice (`soffice`) on PATH for .odg -> .pdf conversion.

pdf_processor_config: pdf_processor_config.yaml
soffice: soffice
